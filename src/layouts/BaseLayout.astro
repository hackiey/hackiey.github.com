---
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import '../styles/global.css';
import 'katex/dist/katex.min.css';

interface Props {
  title?: string;
  description?: string;
}

const {
  title = "Harry's Blog",
  description = 'Notes on code, research, and reading.'
} = Astro.props;

const canonical = Astro.site ? new URL(Astro.url.pathname, Astro.site).toString() : Astro.url.toString();
---

<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={description} />
    <meta name="theme-color" content="#fcfcfa" />
    <link rel="canonical" href={canonical} />
    <link rel="icon" href="/img/favicon.ico" />
    <link rel="alternate" type="application/rss+xml" title="Harry's Blog RSS" href="/rss.xml" />
    <title>{title}</title>
  </head>
  <body>
    <div class="page-shell">
      <Header />
      <main class="page-main">
        <slot />
      </main>
      <Footer />
    </div>
  </body>
</html>
